
* <<Compare old tar file with new tar file.>>

* store old file names in a dictionary

* compare dictionary with new files, if hit,  compare size and mtime

* if 'no hit' print file name in new but not in old

* check not hard link
